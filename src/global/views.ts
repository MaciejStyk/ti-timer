enum views {
  setupPhase = "Setup phase",
  strategyPhase = "Strategy phase",
  actionPhase = "Action phase",
  statusPhase = "Status phase",
  agendaPhase = "Agenda phase",
}

export default views;
